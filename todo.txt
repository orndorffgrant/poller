score
go to results on submit, put something in local storage or cookie
vendor chartscss
handle new_option or put that feature behind flag
make charts prettier

fix edit page submitting
edit poll page: make inputs controlled by alpine, and do extra hidden checkbox true/false trick for booleans

users, prompt to create user/password on first run when no users
only update poll if on correct user
admin can see who voted for who (configurable to make public)

combine all of version 0.1.0 into one migration script, name after version

publish version 0.1.0

edit page: unsaved changes status and leave confirm

some type of prebuild script to make sure tailwind build is correct?